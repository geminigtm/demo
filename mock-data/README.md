# Mock Data (synthetic only)

Purpose: provide fake but realistic inputs to stage the UI for screenshots. Do not reuse production schemas or IDs; keep data minimal and human-readable.

Files:
- `sample-data.json` – example payload covering profile, leads, and outreach drafts.

Guidelines:
- Use Germany → Indonesia scenarios with fictional companies/people. Avoid real brands or personal data.
- Keep numbers rounded and generic (e.g., revenue bands, headcount ranges).
- If you add new files, document their intent here and keep field names descriptive (no internal abbreviations).
- When in doubt, prefer over-simplified data rather than guessing proprietary schema details.
